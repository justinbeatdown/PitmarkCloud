PITMARK CLOUD v0.20.0

1. Extract this ZIP directly into the CURRENT PitmarkCloud repository root.
2. Allow it to replace matching files.
3. Open GitHub Desktop and review the coordinated changes.
4. Commit with the summary below and push. Render can auto-deploy normally.
5. Add the Google Workspace environment variables listed in GOOGLE_WORKSPACE_DEPLOY.md.

This patch was built from live GitHub main at v0.19.10 (commit 0d7919b).
It preserves the v0.19.10 Race Card upload fix, Discord setup, Autopilot, PRT,
Control Center access, database history, and desktop/mobile parity.

GitHub Desktop Summary:
v0.20.0 — Google Workspace / Gmail Migration

Description:
Replace the legacy Pitmark Mail transport and mail subdomain identities with the
Pitmark Racing Co. Google Workspace/Gmail infrastructure. Add Gmail OAuth sending,
background inbox sync, Gmail send-as department identities, Shield scanning, Gmail
read/spam/trash synchronization, authenticated attachment downloads, root-domain
support addresses, updated status UI, and coordinated desktop/mobile behavior.

The included scripts/google_gmail_oauth_setup.py helper creates the one-time Gmail
refresh token without writing it into the repository.
