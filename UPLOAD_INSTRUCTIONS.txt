Extract over the PitmarkCloud repository root, commit/push, and let Render deploy.

Verify Delete Conversation on desktop and mobile. The modal should retain the Pitmark dark/orange design and the Cancel/Delete buttons should no longer render as native white browser buttons.
