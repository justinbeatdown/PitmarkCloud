# Pitmark Cloud v0.14.4 — Direct Facebook Publish Integration

Fixes:
- Facebook Publish button logic now lives directly in Control Center JS.
- APPROVED/SCHEDULED Facebook posts enable Publish when Meta credentials are configured.
- Publish uses the existing server-side Meta publishing endpoint.
- Visible Control Center footer now reports v0.14.4 instead of the stale hardcoded v0.14.0.
- Previous Facebook backend, scheduled worker, Shopify routing, and autofill fixes are preserved.
