# Pitmark Cloud v0.21.10 — Integrated PRT Hub

- Renames the Control Center `PRT Analytics` workspace to `PRT`.
- Integrates Early Access code generation, invite status, copy actions and revocation directly into the PRT workspace.
- Adds licensing summary cards for Free, Early Access, Pro, League / Team and total active entitlements.
- Keeps the existing PRT install/session analytics in the same workspace.
- Retires the standalone Early Access admin page; `/control/early-access` now redirects into Control Center → PRT.
- Restricts Early Access management to Control Center Owner/Admin accounts (or the emergency service admin key).
- Preserves Shopify licensing, device-bound activation, Pitmark Shield, Control Center styling and all existing Cloud workers.
