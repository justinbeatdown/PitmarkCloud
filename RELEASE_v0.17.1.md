# Pitmark Cloud v0.17.1 — PRT Functional Pass

- Adds persistent Mark as Spam training to Pitmark Mail / Shield.
- Trained senders classify as Spam at 99%; non-consumer sender domains are also learned.
- Adds Mark as Spam controls to desktop and mobile mail threads.
- Reworks PRT hero with a real PRT app screenshot and compact status strip.
- Surfaces Pitmark Shield ACTIVE on the PRT website.
- Uses the current Pitmark logo supplied for PRT.
- Adds a direct Discord install launch route so the PRT button performs a server-side redirect into Discord authorization.
