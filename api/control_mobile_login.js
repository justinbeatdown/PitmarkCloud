const $=id=>document.getElementById(id);async function req(url,opt={}){const r=await fetch(url,{...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const t=await r.text();let d;try{d=JSON.parse(t)}catch{d={detail:t}}if(!r.ok)throw Error(d.detail||t);return d}async function login(){try{$('merr').textContent='';await req('/api/control/auth/login',{method:'POST',body:JSON.stringify({username:$('mu').value.trim(),password:$('mp').value})});location.href='/control/mobile'}catch(e){$('merr').textContent=e.message}}$('mlogin').onclick=login;$('mp').addEventListener('keydown',e=>{if(e.key==='Enter')login()});(async()=>{const s=await req('/api/control/auth/state');if(s.authenticated)location.href='/control/mobile';if(s.setup_required){$('merr').textContent='Initialize the Control Center from the desktop login first.';$('mlogin').disabled=true}})().catch(e=>$('merr').textContent=e.message);
