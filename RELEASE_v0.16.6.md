# Pitmark Cloud v0.16.6
Adds a verified public-web fallback for publisher WAFs that return HTTP 202 to both article HTML and WordPress REST. Shield remains in front of direct URL handling; indexed text is accepted only when source-specific host/slug evidence matches.
